namespace JusticeFramework.Core {
    /// <summary>
    /// Quest states that describe if the quest is active, completed, failed, etc
    /// </summary>
    public enum EQuestState {
        Invalid,
        NotStarted,
        InProgress,
        Completed,
        Failed
    }
}